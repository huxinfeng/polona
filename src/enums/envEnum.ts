/** node 环境 */
export enum EnvEnum {
  DEV = 'development',
  TEST = 'test',
  PROD = 'production',
}

/** polona 环境 */
export enum PolonaEnvEnum {
  DEV = 'dev',
  TEST = 'test',
  PROD = 'prod',
  MOCK_LOCAL = 'mock_local',
  MOCK_CLOUD = 'mock_cloud',
}
